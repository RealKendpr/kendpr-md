---
import { Image } from "astro:assets";
const { type, title, description, pubDate, image, url } = Astro.props;
---

<article class="w-full">
  <figure>
    <div
      class="aspect-[16/4] w-full overflow-hidden rounded-lg border border-neutral-200 dark:border-neutral-700"
    >
      <Image
        class="h-full object-cover object-center"
        src={image}
        loading={type === "AllPosts" ? "eager" : "lazy"}
        alt="Kendpr"
        height="960"
        width="1280"
      />
    </div>
    <figcaption class="group mb-2 mt-4 flex items-center justify-between">
      <a class="hidden font-bold group-hover:inline-block" href={url}>{title}</a
      >
      <h3 class="group-hover:hidden">{title}</h3>
      <time
        class="text-xs text-neutral-800 dark:text-neutral-400"
        datetime={pubDate}>{pubDate}</time
      >
    </figcaption>
    <p class="line-clamp-2 !text-sm">
      {description}
    </p>
  </figure>
</article>
